function y = f(x)
    
    %% --- Our Original Function from hw ----------------------------
    y = sin(2*pi*x).*exp(x); % correction to hw from canvas
    % ---------------------------------------------------------------
    
end